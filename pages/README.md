# Alive-AI Pages Portal

Editable source for the GitHub Pages presentation site.

Build output is written to `../docs`, which is what GitHub Pages serves for
`https://vindepemarte.github.io/alive-ai/`.

```bash
cd pages
npm install
npm run build
```

The live site must use the official logo assets from `public/assets/` and must
not point to placeholder social links.
